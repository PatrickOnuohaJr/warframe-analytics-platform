# Cephalon Gu

**Read `Docs/HANDOFF.md` first, before anything else.** It's the pick-up-here note for the current session — where the last session left off, what's next, and what a fresh session needs to know without digging (data-plane-only DB access, gotchas already learned the hard way, open threads). It gets rewritten at the end of each session, so it's always current — this file is just the pointer to it.

For the long-term feature plan (not "what's next right now" but "what's the whole roadmap"), see `Docs/Cephalon_Gu_Master_Roadmap.md`.
